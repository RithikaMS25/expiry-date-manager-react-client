# Implementation Tasks

## 1. UI Setup
- [] Add Tailwind CSS, use #22a6b3 as the primary color, #f0932b as the secondary color, and use other matching colors as and when needed.
- [] Create landing page with header, footer, hero section, and put CTA to login/register. In header, keep the logo (you can generate an appropriate one), login and register link. In hero section, keep a heading about the website, a sub-heading, and CTA to login/register.

## 2. Auth Implementation
- [] Implement Login page and integrate backend API for login. Ensure all fields accepted by backend API is present on the form.
- [] Implement Register page and integrate backend API for register. Ensure all fields accepted by backend API is present on the form.